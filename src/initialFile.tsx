import React, { useState, useEffect } from "react";
import styled from "@emotion/styled";

export const initF: React.FC = () => {
  return (
    <Wrapper>
      <></>
    </Wrapper>
  );
};
const Wrapper = styled.section`
  display: flex;
`;
